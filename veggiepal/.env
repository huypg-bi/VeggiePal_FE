# Vite chỉ nạp biến bắt đầu bằng VITE_ vào import.meta.env
# Trỏ vào api-gateway (VeggiePal_BE), gateway sẽ strip "/api" rồi forward
# sang identity-service (vd: /api/auth/login -> http://localhost:8081/auth/login).
VITE_VEGGIEPAL_API_BASE_URL=http://localhost:8080/api
